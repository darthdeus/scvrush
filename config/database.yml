defaults: &defaults
  adapter: mysql2
  encoding: utf8
  reconnect: false
  pool: 5
  username: root
  password:
  host: localhost

development:
  database: scvrush_development
  <<: *defaults

test: &test
  database: scvrush_test
  <<: *defaults

production:
  database: scvrush_development
  <<: *defaults

cucumber:
  <<: *test