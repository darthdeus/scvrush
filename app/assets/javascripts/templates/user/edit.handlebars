<h2>Activate your account</h2>

<div class="row">
  <p class="span8">
    Once you submit this form your account will be activated.
    You currently have a <strong>trial account, which is fully functional
    but will be automatically deleted on {{expiresIn}} GMT</strong>.
  </p>
</div>

<form class="form-horizontal" {{action "saveProfile" on="submit"}}>

  <div class="control-group">
    <label class="control-label">Username</label>
    <div class="controls">
      {{view Ember.TextField valueBinding="username"}}
    </div>
  </div>

  <div class="control-group">
    <label class="control-label">E-mail</label>
    <div class="controls">
      {{view Ember.TextField valueBinding="email"}}
    </div>
  </div>

  <div {{bindAttr class="passwordInvalid:error :control-group"}}>
    <label class="control-label">Password</label>
    <div class="controls">
      {{view Ember.TextField valueBinding="password" type="password"}}
    </div>
  </div>

  <div {{bindAttr class="passwordConfirmationInvalid:error :control-group"}}>
    <label class="control-label">Password Confirmation</label>
    <div class="controls">
      {{view Ember.TextField valueBinding="passwordConfirmation" type="password"}}
    </div>
  </div>

  <div class="control-group">
    <div class="controls">

      <button type="submit" {{bindAttr disabled="isSaving"}} class="btn btn-primary">
        {{#if isSaving}}
          Saving ...
        {{else}}
          Activate the account
        {{/if}}
      </button>

    </div>
  </div>
</form>
