{{view Em.TextField valueBinding="controller.query" placeholder="Search the posts"}}

<div class="posts">


  {{#each post in controller}}
    <div class="post">
      {{#linkTo "post" post class="image"}}
        <img {{bindAttr src="post.image"}}>
      {{/linkTo}}

      <h2>
        {{#linkTo "post" post}}{{post.title}}{{/linkTo}}
      </h2>
    </div>
  {{/each}}

</div>
