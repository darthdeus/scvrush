Scvrush.UserFollowButton = Ember.View.extend({

  follow: function(user) {
    this.get("controller").send("follow", user);
  },

  follow: function(user) {
    this.get("controller").send("follow", user);
  },

});
